export enum ParameterStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum MessagesParameter {
  NOT_FOUND = 'Parameter not found',
  ALREADY_EXISTS = 'El parametro ya existe',
  NOT_ACTIVE = 'Parametro no activo',
  NOT_UPDATED = 'Parametro no actualizado',
  NOT_DELETED = 'Parametro no eliminado',
}
